module.exports = { //exported in test.js
    chkbtn: function(button) { //functions r tested  chkbtn name of func
        var btn = "click"; //btn given variable 

        if (btn !== null) { //checking it shld not b null
            return btn; //returns var btn
        }
    }
}